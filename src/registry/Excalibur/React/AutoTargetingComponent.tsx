"use client";

import {loadExample} from "@/lib/registry/loadExample";
import {AutoTargetingScene} from "@avalon/Excalibur/Scene/AutoTargetingScene";

const component = loadExample(new AutoTargetingScene());

export default component;